// Cloud AI configuration — swap out model or base URL here

export const CLAUDE_MODEL = 'claude-opus-4-6';

export const API_CONFIG = {
  maxTokens: 1024,
  temperature: 0.7,
};

// Example: if self-hosting or using a proxy, override base URL
// export const ANTHROPIC_BASE_URL = 'https://your-proxy.example.com';
